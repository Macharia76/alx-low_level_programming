#include<stdio.h>
/**
 *main - A program that print a line using print f
 *Return: 0 (Success)
 *printf - sends formatted output to stdout
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
