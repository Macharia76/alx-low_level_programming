# include <stdio.h>
/**
 * main - prints the sum of the even-valued terms
 * Return: 0
 */
int main(void)
{
int b = 0;
long p = 1, s = 2, sum = s;
while (s + p < 4000000)
{
s += p;
if (s % 2 == 0)
sum += s;
j = k - j;
++b;
printf("%ld\n", sum);
return (0);
}

